<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="UziSport.Controls.SupplierViewPopup"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    InputTransparent="False"
    IsVisible="False">

    <Grid
        BackgroundColor="#80000000"
        HorizontalOptions="Fill"
        VerticalOptions="Fill">

        <!--  Popup ở giữa  -->
        <Frame
            Padding="20"
            BackgroundColor="White"
            CornerRadius="10"
            HasShadow="True"
            HeightRequest="400"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            WidthRequest="400">

            <!--  Chia 3 phần: form nhập / line / list  -->
            <Grid RowDefinitions="Auto,*,Auto">
                <!--  Form nhập + nút  -->
                <VerticalStackLayout Grid.Row="0" Spacing="10">
                    <Label
                        FontAttributes="Bold"
                        FontSize="18"
                        HorizontalOptions="Center"
                        Text="Thêm nhà cung cấp" />
                    <Entry
                        x:Name="SupplierNameEntry"
                        Completed="SupplierNameEntry_Completed"
                        Placeholder="Tên nhà cung cấp" />
                    <Label
                        x:Name="SupplierExistsLabel"
                        FontSize="18"
                        HorizontalOptions="Center"
                        IsVisible="False"
                        Text="Hãng này đã tồn tại!"
                        TextColor="Red" />
                    <!--  Line ngăn cách  -->
                    <BoxView
                        BackgroundColor="#DDDDDD"
                        HeightRequest="1"
                        HorizontalOptions="Fill" />
                </VerticalStackLayout>
                <!--  Danh sách nhà cung cấp hiện có  -->
                <CollectionView
                    Grid.Row="1"
                    Margin="0,8,0,0"
                    ItemsSource="{Binding Suppliers}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <!--  Mỗi record: Tên nhà cung cấp + nút X  -->
                            <Grid RowSpacing="2">
                                <Grid
                                    Margin="0,3,0,3"
                                    ColumnDefinitions="*,Auto"
                                    HeightRequest="35">

                                    <Entry
                                        Grid.Column="0"
                                        Margin="2,0,0,0"
                                        HorizontalTextAlignment="Start"
                                        Text="{Binding SupplierName}"
                                        VerticalTextAlignment="Center" />

                                    <Button
                                        Grid.Column="1"
                                        Margin="6,0,0,0"
                                        BackgroundColor="Transparent"
                                        Clicked="DeleteSupplierButton_Clicked"
                                        FontSize="16"
                                        Text="✕"
                                        TextColor="Red"
                                        WidthRequest="40" />
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <VerticalStackLayout Grid.Row="2">
                    <!--  Line ngăn cách  -->
                    <BoxView
                        BackgroundColor="#DDDDDD"
                        HeightRequest="1"
                        HorizontalOptions="Fill" />
                    <HorizontalStackLayout
                        Margin="10"
                        HorizontalOptions="Center"
                        Spacing="40">
                        <Button
                            BackgroundColor="DarkGreen"
                            Clicked="SaveButton_Clicked"
                            Text="Lưu"
                            WidthRequest="90" />
                        <Button
                            BackgroundColor="DarkRed"
                            Clicked="CancelButton_Clicked"
                            Text="Hủy"
                            WidthRequest="90" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Grid>
        </Frame>
    </Grid>
</ContentView>
